# Generated by `npm run build`. Everything in here can be rebuilt from
# the schemas in samples/, so none of it belongs in the repository.
build/

# The README quick start writes here, so following the README should not
# leave a dirty working tree.
out/

# Only the behaviour harness needs anything installed, and it is deliberately
# not saved as a dependency.
node_modules/
